
import React from 'react';
import { TemplateOption, TemplateSelectProps } from './types';

export const TemplateSelect = ({
  selectedTemplate,
  templates,
  onTemplateChange,
  onEdit,
  onSave,
  onRename,
  isEditing = false
}: TemplateSelectProps) => {
  // Componente removido conforme solicitado
  return null;
};
